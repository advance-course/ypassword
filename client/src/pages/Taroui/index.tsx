import Taro from '@tarojs/taro';
import { View } from '@tarojs/components';

export default function UserInfo() {
  return (
    <View>
      把Taro UI小程序的页面完整的搬迁到这里来
    </View>
  )
}

UserInfo.config = {
  "navigationBarTitleText": "Taro UI"
}